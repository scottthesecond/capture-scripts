# Capture Machine Configuration
# Copy this file to .env on your CAPTURE machine and customize as needed

# ===== Stream Configuration =====
# TCP port for the preview stream server (receiver will connect to this port)
STREAM_PORT=1234

# Stream destination URL (automatically uses STREAM_PORT above)
# Default: tcp://0.0.0.0:1234?listen=1 (listens on all interfaces)
# You typically don't need to change this unless you want a custom setup
# STREAM_DEST=tcp://0.0.0.0:1234?listen=1

# Bitrate for preview stream video
BITRATE=6M

# Buffer size for preview stream
BUF_SIZE=12M

# Audio codec for preview stream (when --streamAudio is used)
STREAM_AUDIO_CODEC=mp2

# Audio bitrate for preview stream (when --streamAudio is used)
STREAM_AUDIO_BITRATE=128k

# ===== Video Capture Configuration =====
# Video capture format from your capture device
# Common options: mjpeg, yuyv422, h264
VIDEO_FORMAT=mjpeg

# Video capture resolution
# Common options: 640x480, 1280x720, 1920x1080
VIDEO_SIZE=640x480

# Video capture framerate
# Common options: 25, 30, 60
FRAMERATE=30

# ===== Audio Configuration =====
# Audio codec for recording (not the preview stream)
# Common options: pcm_s16le, pcm_s24le, aac
AUDIO_CODEC=pcm_s16le

# ===== Output Configuration =====
# Default output format for recordings
# Options: prores, prores-lt, hevc
FORMAT=prores-lt

# Default NAS share name (mounted at /mnt/<SHARE>/)
SHARE=Production
