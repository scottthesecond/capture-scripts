# Capture Scripts Environment Configuration
# Copy this file to .env and customize as needed

# UDP stream destination for preview feed
# Format: udp://<ip_address>:<port>
STREAM_DEST=udp://10.0.0.120:1234

# Bitrate for the preview stream
BITRATE=6M

# Stream buffer size
BUF_SIZE=12M

# Video capture format (e.g., mjpeg, yuyv422, h264)
VIDEO_FORMAT=mjpeg

# Video capture resolution (e.g., 640x480, 1280x720, 1920x1080)
VIDEO_SIZE=640x480

# Video capture framerate (e.g., 30, 25, 60)
FRAMERATE=30

# NAS share name (default: Production)
# This is the name of the mounted share at /mnt/<SHARE>/
SHARE=Production

# Default output format (prores, prores-lt, hevc)
FORMAT=prores-lt

# Audio codec for recording (e.g., pcm_s16le, pcm_s24le, aac)
AUDIO_CODEC=pcm_s16le

# Audio codec for stream (e.g., mp2, aac)
STREAM_AUDIO_CODEC=mp2

# Audio bitrate for stream (e.g., 128k, 192k, 256k)
STREAM_AUDIO_BITRATE=128k
